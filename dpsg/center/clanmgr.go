package center
/*
import (
	"logger"
	"proto"
	"rpc"
	"rpcplus"
)

var clans map[string]*clan

make(map[uint64]*player),
func InitClan(name string) (err error) {

}


func HasClan(name string) (err error) {
	ts("CNServer:CreateClan", conn.GetId(), clan)
	defer te("CNServer:CreateClan", conn.GetId(), clan)
	self.l.RLock()
	p, exist := clans[conn.GetId()]
	self.l.RUnlock()
	if !exist {
		return
	}

	
	return
}*/